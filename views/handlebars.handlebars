{{text}}
{{html}}
{{{html}}}
{{! super-secret comment }}


<!--研究块级上下文-->
<ul>
    {{#each tours}}
        {{! this is my block }}
        <li>
            {{name}}-{{price}}
            {{#if ../currencies}}
                ({{../../currency.abbrev}})
            {{/if}}
        </li>
    {{/each}}
</ul>
{{#unless currencies}}
    <p>All prices in {{currency.name}}</p>
{{/unless}}
{{#if specialUrl}}
    <p><a href="{{specialUrl}}">specialUrl</a></p>
{{else}}
    <p>Please check back often for</p>
{{/if}}
<p>
    {{#each currencies}}
        <a href="#">{{.}}</a>
    {{/each}}
</p>